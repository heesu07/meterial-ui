import { Typography } from '@material-ui/core';
import React from 'react';

const Create = (props) => {
  return(
      <div>
        <Typography
          varient='h1'
          color='primary'
        >
          Create a New Note
        </Typography>
      </div>
  );}

export default Create;